# Estado y secretos — NO subir al repositorio
aria_state.json
vtube_token.txt
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Artefactos de runtime del rate limiter / IPC en tasks/ (NO los archivos de
# estado queue/active/completed/stats.json, que sí se trackean intencionalmente).
tasks/rate_gemini.json
tasks/rate_gemini.lock
tasks/*.lock
tasks/*.tmp

# Logs de runs de entrenamiento (locales, no versionar)
logs/

# Config local de Claude Code (por máquina)
.claude/
